import 'package:flutter/material.dart';

class HeartIcon extends StatelessWidget{

  final bool _marked;
  HeartIcon(this._marked);

  @override
  Widget build(BuildContext context){
    return new Container();
  }
}